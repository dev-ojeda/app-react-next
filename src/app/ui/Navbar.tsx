import React from 'react';

const Navbar = () => {
    return (
        <nav style={{ background: '#444', color: '#fff', padding: '15px' }}>
            <h1>Dashboard</h1>
        </nav>
    );
};

export default Navbar;
